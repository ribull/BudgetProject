import React, { useState } from 'react';

import Alert from 'react-bootstrap/Alert';

const PredictPage: React.FC = () => {
  return (
    <Alert>This is the predict page</Alert>
  );
}

export default PredictPage;
