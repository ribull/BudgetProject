import React, { useState } from 'react';

import Alert from 'react-bootstrap/Alert';

const PlanPage: React.FC = () => {
  return (
    <Alert>This is the plan page</Alert>
  );
}

export default PlanPage;
